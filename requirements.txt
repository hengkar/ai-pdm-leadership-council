# AI PDM Leadership Council — pinned loosely; see specs/001-pdm-leadership-council/plan.md
# All installs go through the project venv: .venv/bin/pip install -r requirements.txt

# --- UI ---
gradio>=5.0,<6

# --- Retrieval (local, free at runtime — Constitution II) ---
chromadb>=0.5
sentence-transformers>=3.0        # bge-small-en-v1.5 embeddings + bge-reranker-base
rank-bm25>=0.2.2

# --- LLM providers (user-supplied keys only — Constitution I) ---
anthropic>=0.40
openai>=1.50
google-genai>=1.0

# --- Schemas & config ---
pydantic>=2.7
PyYAML>=6.0

# --- Offline data pipeline ---
requests>=2.32
trafilatura>=1.12                 # HTML article extraction
beautifulsoup4>=4.12              # per-site fallback selectors
pymupdf>=1.24                     # PDF decks
python-frontmatter>=1.1           # podcast transcript YAML headers

# --- Tests ---
pytest>=8.0
